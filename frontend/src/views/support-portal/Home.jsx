import { useState } from 'react';

import useAuth from '../../hooks/useAuth';

const Home = () => {
    const [isAdmin, setAdmin] = useState(false);

    return <div>Home</div>;
};
export default Home;
